# Create a new environment (e.g., named venv)
python3 -m venv venv

# Activate it:
# On Windows:
venv\Scripts\activate

# On Mac/Linux:
source venv/bin/activate

# Then install packages inside this environment
pip install googlemaps python-dotenv